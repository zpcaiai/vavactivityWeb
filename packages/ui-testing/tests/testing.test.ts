import { describe, expect, it } from "vitest"; import { assertSyntheticFixture, horizontalOverflow, visualCaseId } from "../src";
describe("UI evidence helpers", () => { it("detects overflow", () => expect(horizontalOverflow(400, 360)).toBe(40)); it("builds stable case ids", () => expect(visualCaseId({ page: "User Home", viewport: "Mobile 360", theme: "Dark", locale: "zh-CN", density: "comfortable" })).toBe("user-home__mobile-360__dark__zh-cn__comfortable")); it("rejects sensitive fixtures", () => expect(() => assertSyntheticFixture({ email: "real@gmail.com" })).toThrow(/sensitive/u)); });
